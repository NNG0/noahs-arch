# noahs-arch
opinionated arch + hyperland setup

## Setup
run `install.sh` to install setup

## manual setup
run `link-configs.sh` to setup symlinks
run `executables.sh` to make scripts in bin-folder executable

## Missing features
- [ ] style notifications
- [ ] make yazi system file picker
